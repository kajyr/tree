import { CytoscapeEdge, CytoscapeJSON, CytoscapeNode } from './cytoscape';
import { ChildEvent, DeathEvent, Events, LifeEvent, WeddingEvent } from './life-events';

export { CytoscapeEdge, CytoscapeJSON, CytoscapeNode, LifeEvent, WeddingEvent, DeathEvent, ChildEvent, Events };

// To keep some output
export function foo() {
  return 5;
}
